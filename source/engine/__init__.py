"""Formal engine boundaries for the AI Customs ERP platform.

The package wraps the active v2 implementation so new architecture can grow
without breaking the current UI, parser, updater, and release pipeline.
"""

